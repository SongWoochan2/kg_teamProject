<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="seatMapper">

	
	<select id="totalSeat" parameterType="int" resultType="int">
		SELECT count(*) 
		FROM seat
		WHERE show_place_code = #{show_place_code} and seat_type_code != 0
	</select>

	<insert id="insertSeat" parameterType="showPlaceVO">
		INSERT INTO seat 
		(show_place_code, seat_type_code, x_index, y_index)
		VALUES
		(show_place_seq.currval,#{seat_type_code},#{x_index},#{y_index})
	</insert>
	
	<delete id="deleteSeat" parameterType="int">
		DELETE FROM seat WHERE
		show_place_code = #{show_place_code}
	</delete>

</mapper>
