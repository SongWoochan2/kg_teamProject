<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.inquiry">

	<insert id="inquiryWrite" parameterType="inquiryDTO">
		insert into inquiry values 
		(seq_inquiry_code.nextval, #{inquiry_id}, #{inquiry_type},#{inquiry_theater} #{inquiry_title}, #{inquiry_content}, #{inquiry_file},  sysdate)
	</insert>

	<!-- <update id="inquiryModify" parameterType="inquiryDTO">
		update inquiry set inquirytype=#{inquiryType}, inquirytitle=#{inquiryTitle}, inquirycontent=#{inquiryContent}, inquirydate=sysdate where inquirycode=#{inquiryCode}
	</update>
	
	<select id="inquiryList" parameterType="java.util.Map" resultType="inquiryDTO">
		<![CDATA[
		select inquirycode, inquiryid, inquirytype, inquirytitle, inquirycontent, to_char(inquirydate, 'YYYY.MM.DD') as inquirydate 
		from (select rownum rn, tt.* 
				from (select * from inquiry order by inquirycode desc) tt) 
		where rn >= #{startNum} and rn <= #{endNum}
		]]>
	</select>
	
	<select id="inquiryView" parameterType="int" resultType="inquiryDTO">
		select * from inquiry where code=#{code}
	</select>
	
	<update id="updateHit" parameterType="int">
		update inquiry set hit = hit + 1 where code=#{code}
	</update>
	
	<select id="totalA" resultType="int">
		select count(*) from inquiry
	</select>
	
	<delete id="inquiryDelete" parameterType="int">
		delete inquiry where inquirycode=#{code}
	</delete> -->

</mapper>
